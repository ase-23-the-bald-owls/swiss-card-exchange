const imgMaxHeight = 100;
const navbarPadding = 10;
export const styles = {
  navbar: {
    maxHeight: `${imgMaxHeight + navbarPadding}px`,
    navbarPadding,
  },
  logo: {
    height: `${imgMaxHeight}px`,
  },
};
